# TEST overrides for the observability push agent.

# Push credentials ExternalSecret — reads mount-relative Vault path
# test/app/observability-agent (mount = global.vaultKvMount, the shared `secret`).
observability-agent-push-secret:
  externalSecret:
    vaultPath: test/app/observability-agent
