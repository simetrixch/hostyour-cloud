# Public Certificate + IngressRoute for the Kubernetes Dashboard (Headlamp).
# Replaces the previous k8s Ingress + annotation pattern. Auth is handled
# by Headlamp's own OIDC client talking to the IdP — no Traefik forwardAuth
# Middleware needed.
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dashboard-tls
  namespace: kubernetes-dashboard
spec:
  secretName: dashboard-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - kube.example.invalid
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: kubernetes-dashboard
  namespace: kubernetes-dashboard
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`kube.example.invalid`)
      kind: Rule
      services:
        - name: headlamp
          port: 80
  tls:
    secretName: dashboard-tls
