# service-provisioner — the test stage's database host names. Everything else about
# this controller stands in values-common.yaml.
controller:
  mongo:
    host: mongodb-test-headless.mongodb.svc.cluster.local
  redis:
    host: redis-test.redis.svc.cluster.local
  # No postgres.host — postgresql is per-consumer, and the provisioner resolves
  # the host from the claim's own namespace.
