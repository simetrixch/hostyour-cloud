postfix:
  config:
    general:
      # Per-stage selector → DKIM record at dev._domainkey.<apex>; d= stays the apex.
      DKIM_SELECTOR: "dev"

external-secret:
  externalSecret:
    vaultPath: dev/app/postfix
