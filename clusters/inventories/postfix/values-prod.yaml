postfix:
  config:
    general:
      # Per-stage selector → DKIM record at prod._domainkey.<apex>; d= stays the apex.
      DKIM_SELECTOR: "prod"
      mydestination: "$myhostname"

external-secret:
  externalSecret:
    vaultPath: prod/app/postfix
