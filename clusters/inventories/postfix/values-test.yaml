postfix:
  config:
    general:
      # Per-stage selector → DKIM record at test._domainkey.<apex>; d= stays the apex.
      DKIM_SELECTOR: "test"

external-secret:
  externalSecret:
    vaultPath: test/app/postfix
