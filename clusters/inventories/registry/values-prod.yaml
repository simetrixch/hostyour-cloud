# The Vault entries the registry reads on the prod stage. What the registry is, and
# the one cluster of an installation it deploys on, stand in values-common.yaml.
externalsecret-htpasswd:
  externalSecret:
    vaultPath: prod/app/registry

externalsecret-sync-credentials:
  externalSecret:
    vaultPath: prod/app/registry
