# The Vault entries the registry reads on the dev stage. What the registry is, and
# the one cluster of an installation it deploys on, stand in values-common.yaml.
externalsecret-htpasswd:
  externalSecret:
    vaultPath: dev/app/registry

externalsecret-sync-credentials:
  externalSecret:
    vaultPath: dev/app/registry
