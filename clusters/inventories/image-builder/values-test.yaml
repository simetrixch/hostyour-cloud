# TEST-specific image-builder config. The build plane is centralized on ONE
# build cluster; if a TEST cluster carries it, this file wires TEST's Vault
# path for the webhook HMAC secret.
externalsecret-webhooks:
  externalSecret:
    vaultPath: test/app/image-builder
