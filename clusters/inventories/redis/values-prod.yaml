prometheus-redis-exporter:
  redisAddress: redis://redis-prod.redis.svc.cluster.local:6379

# redis-credentials ExternalSecret — reads Vault path prod/app/redis.
externalsecret-redis:
  externalSecret:
    vaultPath: prod/app/redis
