prometheus-redis-exporter:
  redisAddress: redis://redis-dev.redis.svc.cluster.local:6379

# redis-credentials ExternalSecret — reads Vault path dev/app/redis.
externalsecret-redis:
  externalSecret:
    vaultPath: dev/app/redis
