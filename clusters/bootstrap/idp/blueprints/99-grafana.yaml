# yaml-language-server: $schema=
# Authentik blueprint — uses custom YAML tags !Find and !KeyOf that the VS
# Code YAML linter does not recognize. The schema=null directive above
# suppresses its "Unresolved tag" errors. Authentik's own blueprint loader
# (Python yaml.SafeLoader subclass) registers these tags via construct_yaml_str
# fallbacks — see authentik.blueprints.v1.common.BlueprintLoader.
version: 1
metadata:
  name: Grafana OIDC client
  labels:
    blueprints.goauthentik.io/instantiate: "true"
# Wait for 10-groups-scopemapping.yaml's `groups` scope to exist —
# parallel-apply race fix; see 99-argocd.yaml comment for details.
conditions:
  - !Find [authentik_providers_oauth2.scopemapping, [scope_name, groups]]
entries:
  - id: grafana-provider
    model: authentik_providers_oauth2.oauth2provider
    identifiers:
      name: Grafana
    attrs:
      name: Grafana
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
      client_type: confidential
      client_id: grafana
      client_secret: !Env IDP_GRAFANA_CLIENT_SECRET
      redirect_uris:
        - matching_mode: strict
          url: !Format ["https://grafana.%s/login/generic_oauth", !Env IDP_DOMAIN]
      access_token_validity: hours=1
      refresh_token_validity: days=30
      sub_mode: hashed_user_id
      include_claims_in_id_token: true
      grant_types:
        - authorization_code
        - refresh_token
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, groups]]
  - id: grafana-app
    model: authentik_core.application
    identifiers:
      slug: grafana
    attrs:
      name: Grafana
      slug: grafana
      provider: !Find [authentik_providers_oauth2.oauth2provider, [name, Grafana]]
      meta_launch_url: !Format ["https://grafana.%s/", !Env IDP_DOMAIN]
      open_in_new_tab: true
      group: Observability
