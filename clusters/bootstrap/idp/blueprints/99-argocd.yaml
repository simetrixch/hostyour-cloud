# yaml-language-server: $schema=
# Authentik blueprint — uses custom YAML tags !Find and !KeyOf that the VS
# Code YAML linter does not recognize. The schema=null directive above
# suppresses its "Unresolved tag" errors. Authentik's own blueprint loader
# (Python yaml.SafeLoader subclass) registers these tags via construct_yaml_str
# fallbacks — see authentik.blueprints.v1.common.BlueprintLoader.
# Authentik blueprint — OAuth2 provider + Application for ArgoCD.
# Mounted into the Authentik pod at /blueprints/local/99-argocd.yaml via
# ConfigMap. Authentik's worker auto-applies on
# startup; idempotent on re-apply. Env-var interpolation resolves
# ${IDP_*} from the idp-bootstrap Secret mounted as envFrom.
version: 1
metadata:
  name: ArgoCD OIDC client
  labels:
    blueprints.goauthentik.io/instantiate: "true"
# Wait for the `groups` scope mapping from 10-groups-scopemapping.yaml to
# exist before applying this blueprint. Authentik's blueprints_discovery
# enqueues apply_blueprint tasks in parallel; without this gate, a fresh
# install where 99-* runs before 10-* completes lets !Find return None for
# the groups property_mapping, then the serializer rejects `"None"` as an
# invalid scope-mapping pk and the whole blueprint fails. Authentik
# retries skipped blueprints on the next discovery cycle, so eventual
# convergence is guaranteed.
conditions:
  - !Find [authentik_providers_oauth2.scopemapping, [scope_name, groups]]
entries:
  - id: argocd-provider
    model: authentik_providers_oauth2.oauth2provider
    identifiers:
      name: ArgoCD
    attrs:
      name: ArgoCD
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      # RSA keypair → Authentik signs ID tokens with RS256 and publishes
      # the public key at /application/o/<slug>/jwks/. Without this, the
      # provider falls back to HS256 (HMAC with client_secret) and JWKS
      # returns 0 keys — OIDC clients that verify via JWKS (ArgoCD,
      # Grafana, Headlamp) reject the token with "unexpected signature
      # algorithm HS256; expected [RS256]".
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
      client_type: confidential
      client_id: argocd
      client_secret: !Env IDP_ARGOCD_CLIENT_SECRET
      redirect_uris:
        - matching_mode: strict
          url: !Format ["https://argo.%s/auth/callback", !Env IDP_DOMAIN]
      access_token_validity: hours=1
      refresh_token_validity: days=30
      sub_mode: hashed_user_id
      include_claims_in_id_token: true
      grant_types:
        - authorization_code
        - refresh_token
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, groups]]
  - id: argocd-app
    model: authentik_core.application
    identifiers:
      slug: argocd
    attrs:
      name: ArgoCD
      slug: argocd
      provider: !Find [authentik_providers_oauth2.oauth2provider, [name, ArgoCD]]
      meta_launch_url: !Format ["https://argo.%s/", !Env IDP_DOMAIN]
      open_in_new_tab: true
      group: Platform
