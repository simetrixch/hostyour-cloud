# Written by the Manager from the registration of digita-post at prod, the one unit whose SMTP entry is
# attested there: the relay of this stage hands its mail to that entry over the tailnet. Removed when
# no unit of the stage declares one.
postfix:
  config:
    general:
      RELAYHOST: "[100.64.0.2]:2525"
relayTarget:
  address: "100.64.0.2"
  port: 2525
